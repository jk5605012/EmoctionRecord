kind: pipeline
type: docker
name: default
steps:
  - name: test
    image: docker:dind
    environment:
      GOPATH: /drone
      GO111MODULE: "on"
    commands:
      - docker-machine start dev
      - apk update && apk add go
      - go test -v ./...
