kind: pipeline
type: docker
name: default
steps:
  - name: test
    image: docker:dind
    environment:
      GOPATH: /drone
      GO111MODULE: "on"
    commands:
      - apt-get install golang
      - go test -v ./...
